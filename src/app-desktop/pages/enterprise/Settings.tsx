import { PlaceholderPage } from "@/app-desktop/components/shared/PlaceholderPage";

export default function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      description="No real implementation exists in the source app — this will be scoped as new product work in Phase 6."
    />
  );
}
